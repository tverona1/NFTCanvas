/**
 * Type representing json metadata associated with owned area
 */
type AreaMetadata = {
  name: string;
  image?: string;
  description?: string;
  external_url?: string;
}

export default AreaMetadata;